[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.138.2",
    "mlflow>=3.14.0",
    "openai>=2.44.0",
    "pydantic>=2.13.4",
    "transformers>=5.12.1",
]

[tool.fastapi]        
entrypoint = "app:app"